# Dependencias de la app web (Streamlit Cloud)
# Nota: se usa pytesseract (no easyocr) para evitar arrastrar PyTorch,
# que hace fallar la instalación en el plan gratuito de Streamlit Cloud.
streamlit>=1.31.0
streamlit-paste-button>=0.1.2
pytesseract>=0.3.10
pillow>=9.0.0
pandas>=2.0.0
openpyxl>=3.1.0
